# AI agent / IDE config directories
.claude/
.codex/
.agents/
.pi/
.impeccable/

# STT script runtime artifacts (creds / local config / temp)
session.json
accounts.json
config.toml
_tmp-*.m4a
out.srt
out.vtt
out/
webui*.log
webui.pid
.playwright-mcp/
*.png
__pycache__/
*.pyc

# Claude Design handoff bundle (transient import artifact)
*-handoff.zip

# Trellis internal dev workspace (specs, task archives, journals, framework
# scaffold) — kept local, not published. Scrub from history before going public.
.trellis/
.gitattributes
